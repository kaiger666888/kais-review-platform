---
phase: 03-review-frontend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - app/templates/base.html
  - app/templates/pages/dashboard.html
  - app/templates/partials/_review_card.html
  - app/templates/partials/_review_list.html
  - app/templates/partials/_empty_state.html
  - app/templates/partials/_review_detail.html
  - app/templates/partials/_toast.html
  - app/templates/partials/_reject_confirm.html
  - app/web/__init__.py
  - app/web/routes.py
  - app/web/auth.py
  - app/main.py
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-05
  - UI-06

must_haves:
  truths:
    - "Opening / shows a mobile-first dashboard with pending reviews listed as cards"
    - "Tapping a review card opens a full-screen detail overlay with approve and reject buttons"
    - "Approve/reject actions update the review state and show a toast notification"
    - "Bottom tab bar switches between pending/approved/rejected lists"
    - "Layout renders correctly on mobile screens with 44px touch targets"
  artifacts:
    - path: "app/templates/base.html"
      provides: "HTML shell with CDN scripts (HTMX 2.0.9, htmx-ext-sse 2.2.4, Alpine.js 3.15.12, Tailwind v4), bottom tab bar, toast container"
      min_lines: 60
    - path: "app/templates/pages/dashboard.html"
      provides: "Dashboard page extending base with SSE-enabled review list area"
      min_lines: 40
    - path: "app/web/routes.py"
      provides: "Template route handlers: dashboard page, review list partial, review detail partial, approve/reject form handlers"
      exports: ["router"]
    - path: "app/web/auth.py"
      provides: "Template auth dependency reading JWT from httpOnly cookie"
      exports: ["get_template_user"]
  key_links:
    - from: "app/web/routes.py"
      to: "app/core/state_machine.py"
      via: "direct import of transition_state"
      pattern: "from app.core.state_machine import transition_state"
    - from: "app/templates/pages/dashboard.html"
      to: "app/templates/partials/_review_card.html"
      via: "jinja2 include"
      pattern: "{% include"
    - from: "app/web/routes.py"
      to: "app/main.py"
      via: "router registration"
      pattern: "include_router"
---

<objective>
Build the complete template foundation and route handlers for the mobile-first review dashboard.

Purpose: Establish all HTML templates (base, dashboard, partials), template route handlers, cookie-based auth, and HTMX form actions. This plan delivers a fully functional review dashboard with tab switching, review detail overlay, and approve/reject actions — all without real-time SSE (that comes in Plan 02).

Output: Working dashboard at / with review cards, tab navigation, detail overlay, and approve/reject form handlers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-review-frontend/03-CONTEXT.md
@.planning/phases/03-review-frontend/03-UI-SPEC.md
@.planning/phases/03-review-frontend/03-RESEARCH.md

<interfaces>
<!-- Key types and contracts from existing codebase -->

From app/core/auth.py:
```python
def create_jwt(client_id: str, jwt_secret: str, expires_minutes: int = 15) -> str
def decode_jwt(token: str, jwt_secret: str) -> dict
async def require_jwt(request: Request) -> dict
async def get_current_client(payload: dict = Depends(require_jwt)) -> str
async def create_review_token(redis: Redis, review_id: int, ttl: int = 86400) -> str
async def consume_review_token(redis: Redis, token: str) -> int | None
```

From app/core/state_machine.py:
```python
class ReviewState(str, Enum): PENDING, POLICY_EVAL, APPROVING, COMPLETE
class Disposition(str, Enum): AUTO, HUMAN, AI_AUDIT, BLOCK
async def transition_state(db: AsyncSession, review_id: int, from_state: ReviewState, to_state: ReviewState, actor: str, action: str, metadata: dict | None = None) -> Review
```

From app/core/dependencies.py:
```python
def get_redis(request: Request) -> Redis | None
def get_arq_pool(request: Request) -> ArqPool | None
```

From app/core/events.py:
```python
class EventManager:
    def create_connection(self) -> asyncio.Queue
    def remove_connection(self, queue: asyncio.Queue) -> None
    async def broadcast(self, event_data: dict) -> None
    def connection_count(self) -> int
event_manager = EventManager()
async def emit_state_change(db: AsyncSession, review_id: int, from_state: str, to_state: str, actor: str, action: str) -> None
```

From app/models/schemas.py:
```python
class ReviewResponse(BaseModel):
    id: int; review_type: str; content_ref: str; source_system: str; priority: str
    state: ReviewState; disposition: Disposition; version: int
    metadata_: dict | None; created_at: datetime; updated_at: datetime
```

From app/main.py router registration pattern:
```python
app.include_router(auth_router)
app.include_router(reviews_router)
# etc.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies, create template directory, and build all HTML templates</name>
  <files>
    requirements.txt
    app/templates/base.html
    app/templates/pages/dashboard.html
    app/templates/partials/_review_card.html
    app/templates/partials/_review_list.html
    app/templates/partials/_empty_state.html
    app/templates/partials/_review_detail.html
    app/templates/partials/_toast.html
    app/templates/partials/_reject_confirm.html
  </files>
  <read_first>
    app/main.py
    .planning/phases/03-review-frontend/03-UI-SPEC.md
    .planning/phases/03-review-frontend/03-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - requirements.txt contains jinja2-fragments==1.8.0 and python-multipart==0.0.20
    - app/templates/base.html loads HTMX 2.0.9, htmx-ext-sse 2.2.4, Alpine.js 3.15.12, Tailwind v4 CDN
    - app/templates/base.html has bottom tab bar with pending/approved/rejected links using ARIA role="tablist"
    - app/templates/base.html has Alpine.js toast container with @show-toast.window listener
    - app/templates/partials/_review_card.html renders a single review card with Tailwind classes
    - app/templates/partials/_review_detail.html has full-screen overlay with approve/reject footer buttons
    - app/templates/partials/_reject_confirm.html has Alpine.js dialog with required textarea for reason
    - All touch targets are min 44px (min-h-[44px] min-w-[44px])
  </acceptance_criteria>
  <action>
    1. Add jinja2-fragments==1.8.0 and python-multipart==0.0.20 to requirements.txt
    2. Install the new packages: pip install jinja2-fragments==1.8.0 python-multipart==0.0.20
    3. Create directory structure: app/templates/pages/, app/templates/partials/
    4. Create base.html following 03-RESEARCH.md code example EXACTLY:
       - Head: load Tailwind v4 via `https://unpkg.com/@tailwindcss/browser@4`, HTMX 2.0.9, htmx-ext-sse@2.2.4, Alpine.js 3.15.12 (defer)
       - Body: `x-data="{ toasts: [] }"` with `@show-toast.window` listener that pushes to toasts array and auto-removes after 3000ms
       - Toast container: fixed top-4, z-50, with x-for template loop rendering toasts with colored left border
       - Main content area with `class="pb-16"` for bottom tab bar clearance and `{% block content %}{% endblock %}`
       - Fixed bottom nav with `h-14`, 3 tabs (Pending/Approved/Rejected), active tab colored per 03-UI-SPEC.md color system (blue-600 active, gray-500 inactive), ARIA role="tablist" on nav, role="tab" on each link, aria-selected on active
    5. Create partials/_review_card.html:
       - Article element with border-b border-gray-200, p-4 padding
       - Grid layout: thumbnail placeholder (gray-200 bg, rounded), type badge (colored per disposition), source label (text-xs text-gray-500), elapsed time (text-xs text-gray-500)
       - Priority indicator: red dot (w-2 h-2 rounded-full bg-red-600) for critical priority
       - hx-get="/reviews/{id}/detail" hx-target="#detail-overlay" to load detail
       - Click target area covers full card (min-h-[44px])
    6. Create partials/_review_list.html:
       - Container div id="review-list"
       - Loop through reviews rendering _review_card.html via {% include %}
       - "Load more" button at bottom: hx-get with cursor parameter, hx-swap="beforeend"
       - If no reviews: include _empty_state.html
    7. Create partials/_empty_state.html:
       - Per 03-UI-SPEC.md copywriting: heading "All caught up" / "No approved reviews" / "No rejected reviews" based on status variable, body text per spec
    8. Create partials/_review_detail.html:
       - Fixed inset-0 overlay with bg-black/50 backdrop, z-30
       - White slide-up panel from bottom with review content, metadata, audit history placeholder
       - Footer: two buttons side by side — "Approve" (bg-green-600 text-white h-12 w-1/2) and "Reject" (bg-red-600 text-white h-12 w-1/2)
       - Approve: hx-post="/reviews/{id}/approve" hx-target="#review-list" with HX-Trigger for toast
       - Reject: @click opens reject_confirm dialog via Alpine.js
       - Close button at top-right
    9. Create partials/_reject_confirm.html:
       - Alpine.js x-data="{ showReject: false, reason: '' }" overlay
       - Required textarea for reason (min 1 char)
       - "Cancel" and "Reject" buttons — Reject only enabled when reason non-empty
       - Form hx-post="/reviews/{id}/reject" with reason in form data
    10. Create pages/dashboard.html extending base.html:
        - {% block content %} with SSE wrapper div (hx-ext="sse" — SSE URL left as placeholder for Plan 02)
        - Review list area: initial render of _review_list.html with pending reviews
        - Detail overlay container div id="detail-overlay"
        - Reject confirm dialog container
    11. Add Jinja2 custom filter for elapsed time formatting in base.html or register globally:
        - Format as "{n}m ago" / "{n}h ago" / "{n}d ago" from created_at datetime
  </action>
  <verify>
    <automated>python -c "from jinja2_fragments.fastapi import Jinja2Blocks; print('jinja2-fragments OK')" && python -c "import multipart; print('python-multipart OK')"</automated>
  </verify>
  <done>requirements.txt has new deps installed. All template files exist in app/templates/ with correct Tailwind classes, HTMX attributes, Alpine.js bindings, ARIA roles, and 44px touch targets per 03-UI-SPEC.md design contract.</done>
</task>

<task type="auto">
  <name>Task 2: Create web route handlers and register in main.py</name>
  <files>
    app/web/__init__.py
    app/web/routes.py
    app/web/auth.py
    app/main.py
  </files>
  <read_first>
    app/main.py
    app/api/v1/actions.py
    app/api/v1/reviews.py
    app/core/auth.py
    app/core/state_machine.py
    app/core/dependencies.py
    app/models/schemas.py
  </read_first>
  <acceptance_criteria>
    - app/web/__init__.py exists (empty or with imports)
    - app/web/auth.py has get_template_user() dependency reading JWT from httpOnly cookie
    - app/web/auth.py has /t/{token} route for one-time token deep link flow
    - app/web/routes.py has GET / (dashboard), GET /partials/review-list (HTMX partial), GET /reviews/{id}/detail (HTMX partial)
    - app/web/routes.py has POST /reviews/{id}/approve and POST /reviews/{id}/reject HTMX form handlers
    - app/web/routes.py calls transition_state directly (no HTTP self-calls)
    - app/main.py includes web router and configures Jinja2Templates
    - Server starts without import errors: python -m uvicorn app.main:app --port 8090 &
  </acceptance_criteria>
  <action>
    1. Create app/web/__init__.py (empty file)

    2. Create app/web/auth.py:
       - Import create_jwt, decode_jwt from app.core.auth; consume_review_token from app.core.auth
       - Import get_settings from app.core.config; get_redis from app.core.dependencies
       - Import Cookie, HTTPException, Request, Response from fastapi
       - Import RedirectResponse from fastapi.responses
       - `get_template_user(access_token: str | None = Cookie(None))` dependency:
         a. If no cookie, raise HTTPException(401)
         b. Call decode_jwt(access_token, settings.jwt_secret)
         c. Return client string
         d. Wrap decode in try/except — raise 401 on any JWT error
       - `GET /t/{token}` route:
         a. Get redis via get_redis dependency
         b. Call consume_review_token(redis, token)
         c. If None (expired/used): redirect to "/?toast=token_expired" with 303
         d. If valid: create JWT via create_jwt("reviewer", settings.jwt_secret), set httpOnly cookie (key="access_token", max_age=900, samesite="lax"), redirect to "/?detail={review_id}" with 303
         e. Handle Redis unavailable gracefully (HTTPException 503)

    3. Create app/web/routes.py:
       - Import APIRouter, Request, Cookie from fastapi
       - Import HTMLResponse from fastapi.responses
       - Import Jinja2Templates from fastapi.templating
       - Import Jinja2Blocks from jinja2_fragments.fastapi
       - Import json, asyncio
       - Import async_session_maker from app.core.database (or equivalent — check existing import pattern)
       - Import Review model from app.models.database (or equivalent)
       - Import transition_state from app.core.state_machine
       - Import ReviewState from app.models.schemas
       - Import get_template_user from app.web.auth
       - Set up templates and blocks:
         ```python
         templates = Jinja2Templates(directory="app/templates")
         blocks = Jinja2Blocks(directory="app/templates")
         ```
       - Add elapsed_time Jinja2 filter: function taking datetime, returning "{n}m ago" / "{n}h ago" / "{n}d ago"
       - Register filter on templates.env.filters["elapsed_time"]
       - Create router = APIRouter()

       - `GET /` dashboard handler:
         a. Get active_tab from query param (default "pending")
         b. Fetch reviews from DB using async session: query Review where state matches tab (PENDING for pending, COMPLETE+disposition for approved/rejected — check state machine logic)
         c. For pending: state == ReviewState.PENDING
         d. For approved: state == ReviewState.COMPLETE (need to check disposition or action in audit — use a simpler approach: check if last action was "approve")
         e. Actually, use the existing list_reviews API pattern: query by state, order by created_at desc, limit 20
         f. Render full page: templates.TemplateResponse("pages/dashboard.html", {"request": request, "reviews": reviews, "active_tab": active_tab, ...})

       - `GET /partials/review-list` HTMX partial handler:
         a. Accept status query param (default "pending"), cursor query param (optional, for pagination)
         b. Fetch reviews filtered by status, with cursor-based pagination (id < cursor if cursor provided)
         c. Render ONLY the review_list block: blocks.TemplateResponse("pages/dashboard.html", "review_list", {...})
         d. Return HTMLResponse with the partial

       - `GET /reviews/{review_id}/detail` HTMX partial handler:
         a. Fetch review by ID from DB
         b. Render _review_detail.html partial with review data
         c. Return HTML fragment

       - `POST /reviews/{review_id}/approve` HTMX form handler:
         a. Get DB session, fetch review by ID
         b. Call transition_state(db, review_id, ReviewState.APPROVING, ReviewState.COMPLETE, actor="reviewer", action="approve") directly — NO HTTP self-call
         c. Re-fetch updated review list for the active tab
         d. Render review_list block as HTML
         e. Set HX-Trigger header: json.dumps({"showToast": {"message": "Review approved", "type": "success"}})
         f. Return HTMLResponse with fragment and header

       - `POST /reviews/{review_id}/reject` HTMX form handler:
         a. Accept form data with reason field (from python-multipart Form)
         b. Call transition_state directly with reject action and reason in metadata
         c. Re-fetch updated review list
         d. Render review_list block
         e. Set HX-Trigger header: {"showToast": {"message": "Review rejected", "type": "success"}}
         f. Return HTMLResponse

    4. Update app/main.py:
       - Import web router: `from app.web.routes import router as web_router` and `from app.web.auth import router as web_auth_router`
       - After existing include_router calls, add:
         ```python
         app.include_router(web_auth_router)
         app.include_router(web_router)
         ```
       - Note: web routes should be registered AFTER API routes so /api/v1/* takes precedence
       - No template mounting needed — Jinja2Templates reads from directory path directly

    5. Handle the query-string toast on dashboard redirect:
       - In GET / handler, check for query param "toast" — if "token_expired", pass flag to template to show error toast on load
       - In base.html, add Alpine.js x-init that checks for this flag and triggers toast
  </action>
  <verify>
    <automated>cd /home/kai/workspace/kais-review-platform && python -c "from app.web.routes import router; from app.web.auth import router; print('Web routes imported OK')" && python -c "from app.main import app; print('Main app imported OK')"</automated>
  </verify>
  <done>All template routes work: GET / renders dashboard, GET /partials/review-list returns HTMX partials, POST approve/reject call transition_state directly and return HTML with toast headers. One-time token route at /t/{token} exchanges token for JWT cookie and redirects. Main app registers web routers without errors.</done>
</task>

</tasks>

<verification>
1. `python -c "from app.web.routes import router; print('OK')"` — routes import cleanly
2. `python -c "from app.main import app; print('OK')"` — main app starts
3. Start server and verify GET / returns HTML with Tailwind classes (manual spot-check or curl)
</verification>

<success_criteria>
- Dashboard renders at / with mobile-first layout, bottom tab bar, and review cards
- Review detail overlay opens on card click with approve/reject buttons
- Approve action calls transition_state and returns HTML fragment with toast trigger
- Reject action requires reason and calls transition_state
- Tab switching loads different review lists via HTMX partials
- All templates follow 03-UI-SPEC.md design contract
</success_criteria>

<output>
After completion, create `.planning/phases/03-review-frontend/03-01-SUMMARY.md`
</output>
