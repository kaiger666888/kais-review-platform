---
phase: 15-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/core/config.py
  - requirements.txt
  - docker-compose.yml
  - Dockerfile
  - scripts/start.sh
  - .env.example
autonomous: true
requirements:
  - DB-04
  - AUTH-02
  - AUTH-03

must_haves:
  truths:
    - "Docker Compose defines 6 services (api, nginx, redis, postgres, minio, dozzle) with total memory under 1GB"
    - "PostgreSQL container uses timescale/timescaledb image with health check and init SQL mount"
    - "MinIO container runs with memory limit 128M and exposes ports 9000/9001"
    - "API container depends_on postgres (service_healthy) and runs alembic upgrade head before uvicorn"
    - "Settings class loads postgres_url, minio_endpoint, git_repo_url, capability_token_secret from environment"
    - "requirements.txt includes asyncpg, alembic, gitpython, minio and excludes aiosqlite"
  artifacts:
    - path: "app/core/config.py"
      provides: "V2 Settings class with postgres_url, minio_endpoint, git_repo_url, capability_token_secret"
      exports: ["Settings", "get_settings"]
      contains: "postgres_url"
    - path: "requirements.txt"
      provides: "V2 Python dependencies"
      contains: "asyncpg"
    - path: "docker-compose.yml"
      provides: "6-container Docker Compose with PostgreSQL and MinIO"
      contains: "timescale/timescaledb"
    - path: "Dockerfile"
      provides: "Updated API image without SQLite data directory"
      contains: "alembic"
    - path: "scripts/start.sh"
      provides: "Startup script running alembic upgrade head before uvicorn"
      contains: "alembic upgrade head"
    - path: ".env.example"
      provides: "Example environment file with all V2 variables"
      contains: "POSTGRES_PASSWORD"
  key_links:
    - from: "app/core/config.py"
      to: ".env"
      via: "pydantic-settings env_file loading"
      pattern: "postgres_url.*minio_endpoint"
    - from: "docker-compose.yml"
      to: "scripts/init-db.sql"
      via: "volume mount to docker-entrypoint-initdb.d"
      pattern: "init-db\\.sql.*docker-entrypoint-initdb"
    - from: "docker-compose.yml"
      to: "app/core/config.py"
      via: "POSTGRES_URL environment variable"
      pattern: "POSTGRES_URL"
    - from: "scripts/start.sh"
      to: "alembic"
      via: "alembic upgrade head before uvicorn"
      pattern: "alembic upgrade head"
    - from: "requirements.txt"
      to: "app/core/database.py"
      via: "asyncpg driver for PostgreSQL connection"
      pattern: "asyncpg"
---

<objective>
Create the V2 infrastructure layer: Docker Compose expansion, configuration, dependencies, and startup.

Purpose: This plan sets up the runtime environment that the data model (Plan 01) runs inside. PostgreSQL and MinIO containers provide the database and object storage. Updated Settings, requirements, Dockerfile, and startup script wire everything together. Memory budget stays under 1GB (PostgreSQL 256M + API 256M + MinIO 128M + Redis 64M + Nginx 32M = 736M + headroom).

Output: Expanded docker-compose.yml with PostgreSQL + MinIO, V2 Settings class, updated requirements.txt, updated Dockerfile and start.sh.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-foundation/15-CONTEXT.md
@.planning/phases/15-foundation/15-RESEARCH.md

<interfaces>
<!-- Plan 01 creates these files. Plan 02 does NOT touch them, but references their config fields. -->

From app/core/config.py (being rewritten in THIS plan):
```python
# V2 Settings will have:
# postgres_url, redis_url, minio_endpoint, minio_access_key, minio_secret_key,
# minio_bucket, minio_secure, git_repo_url, git_branch, api_key, jwt_secret,
# capability_token_secret, log_level, host, port, review_timeout_minutes,
# ai_audit_timeout_minutes, hot_retention_days, warm_retention_days,
# telegram_bot_token, telegram_allowed_chat_ids
```

From scripts/init-db.sql (created in Plan 01):
- Mounted into PostgreSQL container via docker-entrypoint-initdb.d
- Contains TimescaleDB extension, hypertable, compression, retention setup

Docker Compose memory budget (from RESEARCH.md Pattern 5):
- PostgreSQL: 256M limit, 0.5 CPU
- API: 256M limit, 1.0 CPU
- Redis: 64M limit, 0.25 CPU
- MinIO: 128M limit, 0.25 CPU
- Nginx: 32M limit, 0.25 CPU
- Total: 736M + ~264M headroom = 1GB budget
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Settings class and update requirements.txt</name>
  <files>
    app/core/config.py
    requirements.txt
    .env.example
  </files>
  <read_first>
    app/core/config.py
    requirements.txt
    .planning/phases/15-foundation/15-RESEARCH.md
  </read_first>
  <action>
    FULL REWRITE of config.py and requirements.txt for V2.

    1. **app/core/config.py** -- Replace entire file with V2 Settings per RESEARCH.md Code Example "Updated Settings Class":
       - Keep `from functools import lru_cache` and `from pydantic_settings import BaseSettings, SettingsConfigDict`
       - Settings class fields:
         - `postgres_url: str = "postgresql+asyncpg://review:review@postgres:5432/reviewdb"` -- replaces database_url
         - `redis_url: str = "redis://redis:6379/0"` -- updated default host from localhost to redis (Docker)
         - `minio_endpoint: str = "minio:9000"`
         - `minio_access_key: str = ""`
         - `minio_secret_key: str = ""`
         - `minio_bucket: str = "review-platform"`
         - `minio_secure: bool = False` -- LAN, no TLS
         - `git_repo_url: str = ""` -- GitOps policy repo
         - `git_branch: str = "main"`
         - `api_key: str` -- required, no default
         - `jwt_secret: str` -- required, no default
         - `capability_token_secret: str = ""` -- for Phase 19 token issuance
         - `log_level: str = "INFO"`
         - `host: str = "0.0.0.0"`
         - `port: int = 8000`
         - `review_timeout_minutes: int = 1440` -- 24h human review timeout
         - `ai_audit_timeout_minutes: int = 5` -- 5min AI audit timeout
         - `hot_retention_days: int = 30`
         - `warm_retention_days: int = 365`
         - `telegram_bot_token: str = ""` -- kept for backward compat
         - `telegram_allowed_chat_ids: str = ""`
       - `model_config = SettingsConfigDict(env_file=".env", env_file_encoding="utf-8")`
       - `get_settings()` with `@lru_cache` -- same pattern as V1
       - REMOVE: `database_url` (replaced by postgres_url)

    2. **requirements.txt** -- Full rewrite for V2:
       ```
       fastapi==0.136.1
       uvicorn[standard]==0.46.0
       pydantic==2.13.3
       pydantic-settings==2.14.0
       sqlalchemy[asyncio]==2.0.49
       asyncpg==0.31.0
       alembic==1.18.4
       gitpython==3.1.50
       minio==7.2.20
       redis==5.3.1
       arq==0.28.0
       PyJWT==2.12.1
       PyYAML==6.0.2
       jsonschema==4.23.0
       structlog==25.5.0
       httpx==0.28.1
       jinja2-fragments==1.8.0
       python-multipart==0.0.20
       python-telegram-bot==22.7
       pytest==8.3.5
       pytest-asyncio==0.24.0
       aiohttp==3.13.5
       ```
       CHANGES from V1:
       - ADD: asyncpg==0.31.0, alembic==1.18.4, gitpython==3.1.50, minio==7.2.20
       - REMOVE: aiosqlite==0.22.1
       - Keep all other dependencies at same versions

    3. **.env.example** -- Create example environment file with all V2 variables and comments:
       ```
       # Database
       POSTGRES_URL=postgresql+asyncpg://review:changeme@postgres:5432/reviewdb
       POSTGRES_PASSWORD=changeme

       # Redis
       REDIS_URL=redis://redis:6379/0

       # MinIO
       MINIO_ENDPOINT=minio:9000
       MINIO_ROOT_USER=minioadmin
       MINIO_ROOT_PASSWORD=changeme
       MINIO_ACCESS_KEY=minioadmin
       MINIO_SECRET_KEY=changeme
       MINIO_BUCKET=review-platform

       # Git (GitOps)
       GIT_REPO_URL=
       GIT_BRANCH=main

       # Auth
       API_KEY=changeme
       JWT_SECRET=changeme
       CAPABILITY_TOKEN_SECRET=changeme

       # General
       LOG_LEVEL=INFO
       HOST=0.0.0.0
       PORT=8000

       # Review timeouts
       REVIEW_TIMEOUT_MINUTES=1440
       AI_AUDIT_TIMEOUT_MINUTES=5

       # Retention (days)
       HOT_RETENTION_DAYS=30
       WARM_RETENTION_DAYS=365

       # Telegram (optional)
       TELEGRAM_BOT_TOKEN=
       TELEGRAM_ALLOWED_CHAT_IDS=
       ```
  </action>
  <verify>
    <automated>cd /home/kai/workspace/kais-review-platform && python -c "from app.core.config import Settings, get_settings; s = Settings(api_key='test', jwt_secret='test'); print(f'postgres_url={s.postgres_url}'); assert hasattr(s, 'minio_endpoint'); assert hasattr(s, 'git_repo_url'); assert hasattr(s, 'capability_token_secret'); assert not hasattr(s, 'database_url'); print('Settings OK')" && grep -q "asyncpg" requirements.txt && echo "asyncpg in requirements" && ! grep -q "aiosqlite" requirements.txt && echo "aiosqlite removed" && test -f .env.example && grep -q "POSTGRES_URL" .env.example && echo ".env.example has POSTGRES_URL"</automated>
  </verify>
  <done>
    - config.py has postgres_url, minio_endpoint, git_repo_url, capability_token_secret (AUTH-02)
    - config.py does NOT have database_url (removed)
    - requirements.txt has asyncpg, alembic, gitpython, minio (AUTH-03)
    - requirements.txt does NOT have aiosqlite
    - .env.example documents all V2 environment variables
  </done>
</task>

<task type="auto">
  <name>Task 2: Expand Docker Compose and update Dockerfile + start.sh</name>
  <files>
    docker-compose.yml
    Dockerfile
    scripts/start.sh
  </files>
  <read_first>
    docker-compose.yml
    Dockerfile
    scripts/start.sh
    .planning/phases/15-foundation/15-RESEARCH.md
  </read_first>
  <action>
    FULL REWRITE of Docker Compose, Dockerfile, and start.sh for V2.

    1. **docker-compose.yml** -- Full rewrite with 6 services per RESEARCH.md Pattern 5:

       **postgres service:**
       - image: `timescale/timescaledb:latest-pg16`
       - container_name: review-postgres
       - environment: POSTGRES_USER=review, POSTGRES_PASSWORD=${POSTGRES_PASSWORD}, POSTGRES_DB=reviewdb
       - volumes: postgres_data:/var/lib/postgresql/data, ./scripts/init-db.sql:/docker-entrypoint-initdb.d/init.sql
       - expose: "5432" (not ports -- only internal access)
       - deploy: memory 256M, cpus 0.5
       - healthcheck: pg_isready -U review -d reviewdb, interval 10s, timeout 5s, retries 5, start_period 30s
       - restart: unless-stopped

       **minio service:**
       - image: minio/minio:latest
       - container_name: review-minio
       - command: server /data --console-address ":9001"
       - environment: MINIO_ROOT_USER=${MINIO_ROOT_USER}, MINIO_ROOT_PASSWORD=${MINIO_ROOT_PASSWORD}
       - expose: "9000", "9001"
       - volumes: minio_data:/data
       - deploy: memory 128M, cpus 0.25
       - healthcheck: mc ready local, interval 30s
       - restart: unless-stopped

       **api service** (update existing):
       - REMOVE: `volumes: ./data:/app/data` (no more SQLite bind mount)
       - ADD: depends_on postgres with condition service_healthy (in addition to redis)
       - Keep: memory 256M, read_only, cap_drop ALL, security_opt, healthcheck
       - env_file: .env.production (keep)

       **redis service** -- unchanged (64M, healthcheck, existing config)

       **nginx service** -- unchanged (32M, port 8090:80, existing config)

       **dozzle service** -- unchanged (monitoring profile)

       **volumes:** Add postgres_data and minio_data alongside redis_data

    2. **Dockerfile** -- Update for V2:
       - REMOVE: `RUN mkdir -p /app/data && chown appuser:appuser /app/data` (no SQLite data dir)
       - ADD: Copy alembic.ini and alembic/ directory (needed for migrations)
       - ADD: Copy scripts/init-db.sql (referenced by docker-compose but also available in image)
       - Keep: python:3.12-slim base, non-root user, security hardening
       - Updated COPY instructions:
         ```
         COPY requirements.txt .
         RUN pip install --no-cache-dir -r requirements.txt
         COPY app/ ./app/
         COPY alembic.ini alembic/ ./
         COPY scripts/ ./scripts/
         COPY start.sh /app/start.sh
         RUN chmod +x /app/start.sh
         ```

    3. **scripts/start.sh** -- Update to run Alembic migrations:
       ```bash
       #!/bin/bash
       set -e

       # Run database migrations (PostgreSQL must be ready -- ensured by docker-compose healthcheck)
       alembic upgrade head

       exec "$@"
       ```
       REMOVE: `mkdir -p /app/data` and `chmod 777` (no SQLite data directory)

    NOTE: The Dockerfile should NOT copy scripts/init-db.sql -- it's mounted directly into the PostgreSQL container via docker-compose volume. Only copy alembic files and start.sh into the API image.
  </action>
  <verify>
    <automated>cd /home/kai/workspace/kais-review-platform && docker compose config --quiet 2>&1 && echo "docker-compose.yml valid" && grep -q "timescale/timescaledb" docker-compose.yml && echo "PostgreSQL image found" && grep -q "minio/minio" docker-compose.yml && echo "MinIO image found" && grep -q "alembic upgrade head" scripts/start.sh && echo "start.sh has alembic" && grep -q "alembic" Dockerfile && echo "Dockerfile has alembic" && ! grep -q "mkdir.*data" scripts/start.sh && echo "No SQLite mkdir in start.sh" && ! grep -q "app/data" Dockerfile && echo "No SQLite data dir in Dockerfile"</automated>
  </verify>
  <done>
    - docker-compose.yml has 6 services (api, nginx, redis, postgres, minio, dozzle)
    - PostgreSQL container with timescale/timescaledb image, 256M limit, health check, init SQL mount (DB-04)
    - MinIO container with 128M limit, exposes 9000/9001
    - API container depends on postgres (service_healthy) and redis (service_healthy)
    - API container has no SQLite data volume mount
    - Total memory: 256+256+64+128+32 = 736M < 1GB
    - Dockerfile copies alembic files, no SQLite data directory
    - start.sh runs `alembic upgrade head` before uvicorn
  </done>
</task>

</tasks>

<verification>
1. Docker Compose config is valid: `docker compose config --quiet`
2. PostgreSQL service exists: `grep "timescale/timescaledb" docker-compose.yml`
3. MinIO service exists: `grep "minio/minio" docker-compose.yml`
4. API depends on postgres: `grep -A5 "depends_on" docker-compose.yml | grep postgres`
5. Memory budget under 1GB: sum all deploy.resources.limits.memory values
6. Settings has all new fields: `python -c "from app.core.config import Settings; s = Settings(api_key='x', jwt_secret='x'); assert s.postgres_url; assert s.minio_endpoint"`
7. aiosqlite removed: `! grep aiosqlite requirements.txt`
8. asyncpg added: `grep asyncpg requirements.txt`
9. start.sh runs alembic: `grep "alembic upgrade head" scripts/start.sh`
10. No SQLite references in Dockerfile: `! grep "app/data" Dockerfile`
</verification>

<success_criteria>
- Docker Compose defines PostgreSQL + MinIO + API + Redis + Nginx + Dozzle, total memory under 1GB
- PostgreSQL uses TimescaleDB image with health check and init SQL volume mount
- MinIO configured with memory limit and console address
- API container starts after PostgreSQL is healthy, runs Alembic migrations
- Settings class has all V2 config fields (postgres_url, minio_endpoint, git_repo_url, capability_token_secret)
- requirements.txt has asyncpg/alembic/gitpython/minio, no aiosqlite
- Dockerfile has no SQLite data directory setup
- start.sh runs alembic upgrade head before application startup
</success_criteria>

<output>
After completion, create `.planning/phases/15-foundation/15-02-SUMMARY.md`
</output>
