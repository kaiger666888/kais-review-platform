---
phase: 03-review-frontend
plan: 03
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - app/web/auth.py
  - app/templates/pages/token_redirect.html
  - app/templates/base.html
  - app/web/routes.py
autonomous: false
requirements:
  - UI-04

must_haves:
  truths:
    - "Opening /t/{token} on a phone validates the one-time token and redirects to dashboard with review detail open"
    - "Expired/used tokens redirect to dashboard with error toast"
    - "JWT is set as httpOnly cookie during token exchange"
    - "Review detail overlay auto-opens when ?detail={id} query param is present"
  artifacts:
    - path: "app/web/auth.py"
      provides: "One-time token deep link route with JWT cookie exchange"
      contains: "/t/{token}"
    - path: "app/templates/pages/token_redirect.html"
      provides: "Intermediate redirect page for token processing"
      min_lines: 10
  key_links:
    - from: "app/web/auth.py"
      to: "app/core/auth.py"
      via: "consume_review_token + create_jwt"
      pattern: "consume_review_token"
    - from: "/t/{token}"
      to: "/?detail={review_id}"
      via: "303 redirect after JWT cookie set"
      pattern: "RedirectResponse"
---

<objective>
Complete the one-time token deep link flow end-to-end.

Purpose: A reviewer receives a one-time link on their phone. Opening it validates the token, sets a JWT cookie, and redirects to the dashboard with the review detail overlay open — enabling one-tap approval from a notification link.

Output: Fully working /t/{token} deep link flow with JWT cookie auth, auto-open detail overlay, and error handling for expired/used tokens.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-review-frontend/03-RESEARCH.md
@.planning/phases/03-review-frontend/03-01-SUMMARY.md

<interfaces>
From app/core/auth.py:
```python
def create_jwt(client_id: str, jwt_secret: str, expires_minutes: int = 15) -> str
async def consume_review_token(redis: Redis, token: str) -> int | None
```

From app/core/dependencies.py:
```python
def get_redis(request: Request) -> Redis | None
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement token deep link route, detail auto-open, and error toast flow</name>
  <files>
    app/web/auth.py
    app/templates/pages/token_redirect.html
    app/templates/base.html
    app/web/routes.py
  </files>
  <read_first>
    app/web/auth.py
    app/web/routes.py
    app/core/auth.py
    app/core/dependencies.py
    app/templates/base.html
    app/templates/pages/dashboard.html
    app/templates/partials/_review_detail.html
  </read_first>
  <acceptance_criteria>
    - app/web/auth.py has GET /t/{token} route that calls consume_review_token
    - On valid token: sets httpOnly cookie "access_token", redirects to /?detail={review_id} with 303
    - On invalid token: redirects to /?toast=token_expired with 303
    - app/web/routes.py GET / handler checks ?detail= query param and passes review_id to template
    - Dashboard template auto-opens detail overlay when detail_id is provided
    - Dashboard template shows toast on load when ?toast= query param is present
    - Server starts without errors
  </acceptance_criteria>
  <action>
    1. Verify/update app/web/auth.py /t/{token} route (created in Plan 01 but verify completeness):
       - Must call consume_review_token(redis, token)
       - On success: create_jwt("reviewer", settings.jwt_secret), set httpOnly cookie, redirect 303 to /?detail={review_id}
       - On failure: redirect 303 to /?toast=token_expired
       - Cookie settings: key="access_token", httponly=True, max_age=900 (15min), samesite="lax"

    2. Update app/web/routes.py GET / handler:
       - Accept detail query param (optional int): `detail: int | None = Query(None)`
       - Accept toast query param (optional str): `toast: str | None = Query(None)`
       - Pass both to template context: {"detail_id": detail, "toast_message": toast}
       - If toast == "token_expired", pass toast_type="error" and message "This link has expired or been used."

    3. Update app/templates/pages/dashboard.html to support auto-open detail:
       - If detail_id is set, render _review_detail.html immediately (pre-loaded) instead of empty detail-overlay div
       - This means the detail overlay shows on page load when coming from a deep link

    4. Update app/templates/base.html to support query-string toast on load:
       - Add Alpine.js x-init on body or a wrapper div that checks window.location.search for toast parameter
       - If toast param exists, push toast to the toasts array on load
       - Alternative: pass toast_message from route handler and use x-init to trigger

    5. Create app/templates/pages/token_redirect.html (optional intermediate page):
       - Minimal "Redirecting..." page in case the 303 redirect has any delay
       - Likely NOT needed since 303 redirect is handled by the browser natively, but create as a safety net

    6. Ensure the detail overlay auto-opens:
       - In dashboard.html, conditionally render the detail overlay content when detail_id is provided
       - The overlay should be visible by default (no hx-get needed — data is pre-rendered)
       - Use the same _review_detail.html partial, pre-populated with the review data
  </action>
  <verify>
    <automated>cd /home/kai/workspace/kais-review-platform && python -c "from app.web.auth import router; print('Auth router OK')" && grep -q "detail" app/templates/pages/dashboard.html && echo "Detail param handled" && grep -q "token_expired\|toast" app/web/routes.py && echo "Toast handling OK"</automated>
  </verify>
  <done>One-time token deep link works: /t/{token} validates token, sets JWT cookie, redirects to dashboard with detail overlay auto-opened. Expired tokens redirect with error toast. Dashboard handles ?detail= and ?toast= query params.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify one-time token deep link flow end-to-end</name>
  <files>none</files>
  <action>Wait for human to verify the complete one-time token flow by opening /t/{token} in a browser, confirming auto-open detail overlay, approve action, and expired token error handling.</action>
  <what-built>Complete one-time token deep link flow: token validation, JWT cookie exchange, auto-open review detail, error handling for expired tokens.</what-built>
  <how-to-verify>
    Prerequisites: Redis must be running (required for one-time tokens).

    1. Start the server: `uvicorn app.main:app --port 8090`
    2. Create a review via API: `curl -X POST http://localhost:8090/api/v1/reviews -H "Content-Type: application/json" -H "Authorization: Bearer <jwt>" -d '{"review_type":"test","content_ref":"test.jpg","source_system":"test","priority":"medium"}'`
    3. Generate a one-time token via the review's action endpoint
    4. Open the token URL in a browser: `http://localhost:8090/t/<token>`
    5. Verify: redirected to dashboard, review detail overlay is open, approve/reject buttons visible
    6. Click Approve — verify toast "Review approved" appears
    7. Open the same token URL again — verify error toast "This link has expired or been used."
  </how-to-verify>
  <resume-signal>Type "approved" if the flow works, or describe what needs fixing.</resume-signal>
  <verify>Human confirms token flow works end-to-end</verify>
  <done>Human has verified one-time token deep link flow works correctly on mobile browser.</done>
</task>

</tasks>

<verification>
1. `python -c "from app.web.auth import router; print('OK')"` — auth route imports
2. `grep "consume_review_token" app/web/auth.py` — token consumption call exists
3. `grep "detail" app/templates/pages/dashboard.html` — detail param handled
4. `grep "set_cookie" app/web/auth.py` — JWT cookie is set
</verification>

<success_criteria>
- /t/{token} validates one-time token and sets JWT in httpOnly cookie
- Valid token redirects to /?detail={id} with detail overlay pre-opened
- Expired/used token redirects to /?toast=token_expired with error toast
- Dashboard auto-opens detail overlay when detail query param present
- Approve/reject actions work from the deep link detail view
</success_criteria>

<output>
After completion, create `.planning/phases/03-review-frontend/03-03-SUMMARY.md`
</output>
