---
phase: 23-review-template-system
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - app/web/routes.py
  - app/templates/partials/_template_wrapper.html
  - app/templates/partials/_template_candidate_grid.html
  - app/templates/partials/_template_risk_assessment.html
  - app/templates/partials/_decision_panel.html
  - app/models/schemas.py
  - app/api/v1/mobile.py
  - app/templates/partials/_mobile_card.html
  - tests/test_template_rendering.py
autonomous: true
requirements: [INTEGRATION-4B.1]

must_haves:
  truths:
    - "Desktop workstation renders template-aware decision panel based on source_system + phase"
    - "movie-agent art-direction shot cards display candidate grid with scores and selection buttons"
    - "gold-team task-parameter shot cards display risk assessment with progress bar"
    - "Mobile API returns template_config in MobileShotCardBundle response"
    - "Mobile PWA card uses template_config.card_variant for conditional display"
    - "Unknown source_system/phase combinations render default decision panel without errors"
  artifacts:
    - path: "app/templates/partials/_template_wrapper.html"
      provides: "Jinja2 wrapper that dynamically includes template-selected partials"
      min_lines: 15
    - path: "app/templates/partials/_template_candidate_grid.html"
      provides: "Movie-agent candidate side-by-side grid with scores"
      contains: "candidate"
    - path: "app/templates/partials/_template_risk_assessment.html"
      provides: "Gold-team risk assessment display with task parameters"
      contains: "risk_score"
    - path: "app/models/schemas.py"
      provides: "MobileShotCardBundle with template_config field"
      contains: "template_config"
  key_links:
    - from: "app/web/routes.py"
      to: "app/core/template_registry.py"
      via: "TemplateRegistry.resolve() in shot_card_detail_partial handler"
      pattern: "template_registry\\.resolve|derive_source_system"
    - from: "app/api/v1/mobile.py"
      to: "app/core/template_registry.py"
      via: "TemplateRegistry.resolve() in _shot_card_to_bundle helper"
      pattern: "template_registry\\.resolve|template_config"
    - from: "app/templates/partials/_template_wrapper.html"
      to: "app/templates/partials/_template_candidate_grid.html"
      via: "Jinja2 dynamic include with variable path"
      pattern: "include template\\.desktop_decision_panel"
---

<objective>
Wire the TemplateRegistry into desktop and mobile rendering paths, creating template-specific HTML partials and modifying route handlers to select templates based on source_system + phase.

Purpose: This connects the template infrastructure from Plan 01 to the actual rendering endpoints. Desktop workstation uses server-side Jinja2 include selection via a wrapper partial. Mobile API passes template_config metadata in JSON responses for Alpine.js conditional rendering. Two new specialized partials provide movie-agent candidate grid and gold-team risk assessment displays.

Output: Template-aware desktop rendering, mobile API with template metadata, candidate grid and risk assessment partials, integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-review-template-system/23-RESEARCH.md
@.planning/phases/23-review-template-system/23-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: TemplateRegistry exports -->

From app/core/template_registry.py (created in Plan 01):
```python
@dataclass
class TemplateConfig:
    desktop_decision_panel: str = "partials/_decision_panel.html"
    desktop_media_player: str = "partials/_media_player.html"
    mobile_card_variant: str = "default"
    show_scores: bool = False
    show_candidates: bool = True
    candidate_layout: str = "grid"
    extra_fields: dict = field(default_factory=dict)

class TemplateRegistry:
    def load_from_directory(self, dirpath: str) -> list[str]: ...
    def resolve(self, source_system: str, phase: str | None = None) -> TemplateConfig: ...

def derive_source_system(shot_card) -> str: ...
def get_template_registry() -> TemplateRegistry: ...
```

From app/web/routes.py (existing handler to modify):
```python
@router.get("/partials/shot-card-detail/{shot_card_id}", response_class=HTMLResponse)
async def shot_card_detail_partial(request: Request, shot_card_id: int):
    # Currently: returns _decision_panel.html directly
    # Must become: resolve template, return _template_wrapper.html with template context
```

From app/api/v1/mobile.py (existing helper to modify):
```python
def _shot_card_to_bundle(shot_card: ShotCard) -> MobileShotCardBundle:
    # Must become: add template_config dict to bundle
```

From app/models/schemas.py:
```python
class MobileShotCardBundle(BaseModel):
    id: int
    shot_id: str
    project_id: str
    # ... existing flat fields ...
    audit_status: str
    routing_decision: str | None = None
    # ADD: template_config field
```

From app/templates/partials/_decision_panel.html:
- Renders narrative context, prompts, node status, provenance, approve/reject buttons
- Ends with OOB swap for #media-preview that includes _media_player.html
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create template wrapper, candidate grid, and risk assessment partials + wire TemplateRegistry into desktop routes and mobile API</name>
  <files>app/templates/partials/_template_wrapper.html, app/templates/partials/_template_candidate_grid.html, app/templates/partials/_template_risk_assessment.html, app/web/routes.py, app/models/schemas.py, app/api/v1/mobile.py, app/templates/partials/_mobile_card.html, tests/test_template_rendering.py</files>
  <read_first>
    app/web/routes.py (shot_card_detail_partial handler at line 332 -- currently returns _decision_panel.html directly, must use TemplateRegistry)
    app/api/v1/mobile.py (_shot_card_to_bundle helper at line 41, MobileShotCardBundle usage)
    app/models/schemas.py (MobileShotCardBundle class at line 338 -- add template_config field)
    app/templates/partials/_decision_panel.html (current default decision panel with OOB swap pattern)
    app/templates/partials/_media_player.html (current media player with candidate grid)
    app/templates/partials/_mobile_card.html (current mobile card with Alpine.js rendering)
    app/core/template_registry.py (TemplateRegistry, TemplateConfig, derive_source_system, get_template_registry)
  </read_first>
  <behavior>
    - Test 1: GET /partials/shot-card-detail/{id} for movie-agent shot card returns HTML containing candidate grid elements
    - Test 2: GET /partials/shot-card-detail/{id} for gold-team shot card returns HTML containing risk assessment elements
    - Test 3: GET /partials/shot-card-detail/{id} for unknown source_system returns default decision panel HTML
    - Test 4: GET /api/v1/mobile/cards returns items with template_config dict containing card_variant
    - Test 5: Template wrapper renders the include path from TemplateConfig.desktop_decision_panel
  </behavior>
  <action>
    1. Create app/templates/partials/_template_wrapper.html:
       - Accepts "shot" and "template" (TemplateConfig) as context variables
       - Uses Jinja2 {% include template.desktop_decision_panel ignore missing with context %} for dynamic partial selection
       - Includes the OOB swap div for #media-preview with {% include template.desktop_media_player ignore missing with context %}
       - Wraps everything in a div with data-template attribute for debugging

    2. Create app/templates/partials/_template_candidate_grid.html:
       - Movie-agent specific partial for side-by-side candidate display
       - Renders shot.visual_bundle.candidates as a grid (2 columns for side-by-side layout, 3 for grid)
       - Each candidate shows: first frame thumbnail, candidate index, score badge (green if >= 0.7, yellow if >= 0.5)
       - Selection buttons: "Select & Approve" button posting to /shot-cards/{id}/approve with selected candidate_id in hx-vals
       - Standard reject button (reuse from _decision_panel.html pattern)
       - Includes narrative context section (scene, shot_number, emotion_curve, continuity_tags) at the top
       - Includes OOB swap for #media-preview at the bottom

    3. Create app/templates/partials/_template_risk_assessment.html:
       - Gold-team specific partial for task parameters + risk display
       - Task Parameters section: shows narrative_context.task_type as badge, narrative_context.task_params as key-value list
       - Risk Assessment section: progress bar using narrative_context.risk_score (0-1 range), color-coded (red >= 0.7, yellow >= 0.3, green < 0.3)
       - Standard approve/reject buttons (same pattern as _decision_panel.html)
       - Includes narrative context section and prompts
       - Includes OOB swap for #media-preview at the bottom

    4. Modify app/web/routes.py -- shot_card_detail_partial function:
       - Import derive_source_system and get_template_registry from app.core.template_registry
       - After fetching shot_card, call derive_source_system(shot_card) to get source_system
       - Extract phase from shot_card.narrative_context.get("phase") or shot_card.narrative_context.get("pipeline_phase")
       - Call get_template_registry().resolve(source_system, phase) to get TemplateConfig
       - Return templates.TemplateResponse with _template_wrapper.html, passing {"shot": shot_card, "template": template_config}
       - Initialize the template registry at module level or in a startup function: load app/templates/config/ directory

    5. Modify app/models/schemas.py -- MobileShotCardBundle:
       - Add field: template_config: dict | None = None (default None for backward compat)
       - The dict will contain {"card_variant": str, "show_scores": bool, "show_candidates": bool}

    6. Modify app/api/v1/mobile.py -- _shot_card_to_bundle:
       - Import derive_source_system and get_template_registry
       - After extracting flat fields, derive source_system from shot_card
       - Extract phase from narrative_context
       - Resolve template: get_template_registry().resolve(source_system, phase)
       - Set template_config={"card_variant": tc.mobile_card_variant, "show_scores": tc.show_scores, "show_candidates": tc.show_candidates}

    7. Modify app/templates/partials/_mobile_card.html:
       - Add Alpine.js conditional section that checks cards[currentIndex].template_config
       - When template_config.card_variant === "candidate-swipe": show candidate switcher UI (existing candidate dots already present, add score display when show_scores is true)
       - When template_config.card_variant === "risk-assessment": show risk score bar and task type badge
       - Default variant renders as currently (no changes to existing behavior)

    8. Create tests/test_template_rendering.py:
       - Use FastAPI TestClient with template registry pre-loaded from test YAML configs
       - Test that movie-agent shot card detail returns candidate grid HTML
       - Test that gold-team shot card detail returns risk assessment HTML
       - Test that unknown source_system returns default panel
       - Test mobile API response includes template_config field
  </action>
  <verify>
    <automated>cd /home/kai/workspace/kais-review-platform && python -m pytest tests/test_template_rendering.py tests/test_template_registry.py -v --tb=short 2>&1 | tail -40</automated>
  </verify>
  <done>
    - Desktop shot_card_detail_partial uses TemplateRegistry to select template-aware partials
    - Movie-agent shot cards render candidate grid with scores and selection buttons
    - Gold-team shot cards render risk assessment with task parameters
    - Unknown source_system falls back to default decision panel (existing _decision_panel.html)
    - Mobile API MobileShotCardBundle includes template_config dict
    - Mobile card template conditionally renders based on template_config.card_variant
    - All template rendering tests pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML config -> Jinja2 include path | Include paths validated in Plan 01 (partials/ prefix required) |
| Mobile API response -> Alpine.js | Template config is display metadata, no security impact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-04 | Tampering | Dynamic Jinja2 include in _template_wrapper.html | mitigate | Include paths come from TemplateRegistry (developer-controlled YAML), not user input. Plan 01 validates partials/ prefix. |
| T-23-05 | Spoofing | Template config in mobile API response | accept | Template config is display metadata only (card_variant, show_scores). Does not affect authorization or data integrity. |
| T-23-06 | Information Disclosure | Template rendering errors | mitigate | Catch template rendering errors gracefully, fall back to default partial, log error via structlog. |
</threat_model>

<verification>
1. GET /partials/shot-card-detail/{id} returns template-selected HTML based on source_system + phase
2. movie-agent candidate grid shows candidates with scores and selection buttons
3. gold-team risk assessment shows task parameters and risk progress bar
4. Default template renders correctly for unknown source_system
5. GET /api/v1/mobile/cards response items include template_config dict
6. All template rendering tests pass
7. Existing workstation and mobile functionality unaffected (backward compatible)
</verification>

<success_criteria>
- Desktop workstation renders different UI for movie-agent vs gold-team vs unknown source systems
- movie-agent art-direction phase shows side-by-side candidate grid with scores
- gold-team task-parameter phase shows risk assessment with progress bar
- Mobile API returns template_config for client-side conditional rendering
- Mobile PWA card conditionally displays candidate switcher or risk assessment based on card_variant
- Unknown source_system/phase combinations gracefully fall back to default template
- All existing workstation and mobile tests still pass
</success_criteria>

<output>
Create `.planning/phases/23-review-template-system/23-02-SUMMARY.md` when done
</output>
